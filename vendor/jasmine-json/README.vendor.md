# vendor/jasmine-json

Files taken from [`pulsar-edit/jasmine-json`](https://github.com/pulsar-edit/jasmine-json/).

Even though we only care about `./lib/matchers.js`, the rest of the repo has been
added, along with the original CoffeeScript, so that future work could hopefully decaf
and simplfy this with easy access to the original source files.
