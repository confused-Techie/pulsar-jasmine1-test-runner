# vendor/jasmine-tagged

Files taken from [`pulsar-edit/jasmine-tagged`](https://github.com/pulsar-edit/jasmine-tagged).

Even though we only care about `./lib/jasmine-tagged.js`, the rest of the repo has been
added, along with the original CoffeeScript, so that future work could hopefully decaf
and simplfy this with easy access to the original source files.
